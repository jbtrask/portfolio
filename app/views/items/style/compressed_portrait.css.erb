#css_info {
	background-color: #008000;
	color: #FFFFFF;
}

#sidebar {
  width: 100%;
  min-width: 100%;
  height: <%= 100.0 - CONTENT_PERCENT['compressed'] %>%;
  min-height: <%= 100.0 - CONTENT_PERCENT['compressed'] %>%;
}

#main {
  top: <%= 100.0 - CONTENT_PERCENT['compressed'] %>%;
  left: 0;
  width: 100%;
  min-width: 100%;
  height: <%= CONTENT_PERCENT['compressed'] %>%;
  min-height: <%= CONTENT_PERCENT['compressed'] %>%;
}

<%= render :partial => "items/style/smart_sizes", :locals => { :name => 'compressed portait', :rows => 4, :columns => 6, :vertical_content => CONTENT_PERCENT['compressed'], :horizontal_content => 100.0 } %>
